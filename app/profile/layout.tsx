import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'โปรไฟล์ • AI Portal' }

export default function ProfileLayout({ children }: { children: React.ReactNode }){
	return children as any
}
